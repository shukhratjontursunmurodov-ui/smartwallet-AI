version: '3.8'

services:
  smart-wallet-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smart-wallet-ai
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
